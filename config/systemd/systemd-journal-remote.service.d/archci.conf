# Plain HTTP: the socket listens on 127.0.0.1 only (the socket drop-in); the
# workers reach it through their ssh tunnel to the master.
[Service]
ExecStart=
ExecStart=/usr/lib/systemd/systemd-journal-remote --listen-http=-3 --output=/var/log/journal/remote/archci.journal
