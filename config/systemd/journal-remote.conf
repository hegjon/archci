# /etc/systemd/journal-remote.conf.d/archci.conf -- worker journals on the master
[Remote]
Seal=false
# one file per worker: /var/log/journal/remote/remote-<hostname>.journal
SplitMode=host
MaxUse=2G
MaxFiles=200
