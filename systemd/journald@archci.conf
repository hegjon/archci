# journald instance for the "archci" journal namespace, where the worker and
# build units log (LogNamespace=archci in their units). Kept apart from the
# machine's own journal so only archci output is streamed to the master and so
# full build logs get their own size limit.
[Journal]
Storage=persistent
SystemMaxUse=4G
RateLimitIntervalSec=0
